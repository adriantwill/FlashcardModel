fine tunes qwen3-vl-8b using pdf slideshow pngs to make question/answer text. it uses core library building blocks (torch, transformers, and peft) to construct a fully custom, hand-coded architecture—including an explicit torch.utils.data.Dataset pipeline for multi-modal alignment, a manually configured 4-bit QLoRA adapter injection, a raw Python training loop with explicit backpropagation and native mixed precision (torch.amp)
